import React from 'react';
import JokeModal from 'components/Modal';

const Landing: React.FC = () => {
  return (
    <React.Fragment>
      <JokeModal />
    </React.Fragment>
  );
};

export default Landing;
